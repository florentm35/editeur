package fr.florent.model.editeur.layer;

import fr.florent.model.editeur.tile.Tile;

public class TileLayer extends Layer<Tile> {
    public TileLayer(double width, double height) {
        super(width, height);
    }
}
